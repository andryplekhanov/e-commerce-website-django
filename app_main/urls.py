from django.urls import path
from .views import *

# urlpatterns = [
#     path('', GoodListView.as_view(), name='index'),
#     path('best_sales/', BestSalesView.as_view(), name='best_sales'),
#     path('loyalty/', loyalty_view, name='loyalty'),
#     path('<slug:good_slug>/', product_detail, name='good_detail'),
# ]